import { ControlsContainer } from "../../../Styled-Components/layoutStyles/controls/controls"

export const Controls = ({ children }) => {
    return (
    <ControlsContainer>
        { children }
    </ControlsContainer>)
}
